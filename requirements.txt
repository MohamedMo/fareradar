# Core (required)
httpx>=0.27
aiosqlite>=0.20

# Dashboard API (src/api.py)
fastapi>=0.110
uvicorn[standard]>=0.29

# Community monitoring (optional but recommended)
feedparser>=6.0

# Google Flights scraper (optional — Phase 4)
# fast-flights>=1.0
